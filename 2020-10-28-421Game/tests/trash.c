

// int[4] dicesFixed = [0, 0, 0]
int dice1Fixed = 1;
int dice2Fixed = 1;
int dice3Fixed = 1;

if (dice1 == dice2 && dice1 == dice3) {
  // all dices have the same value
  dice1Fixed = 0;
  dice2Fixed = 0;
  dice3Fixed = 0;
} else if () {
  // two dices have the same value
}